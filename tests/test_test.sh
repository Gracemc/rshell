touch newfile
touch file2
[ -d newfile ]
test -f newfile
test -e newfile && echo "path exists" || test -e file2
test -e file || test -e file2
[ -e newfile ] && echo path exists
[ -e newfile ] ]
[ [ -e newfile ] ]
